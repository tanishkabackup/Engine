export enum Roles{
    Developer = "Developer",
    ProjectManager = "ProjectManager",
}

export enum Priority{
    High = "High",
    Medium = "Medium",
    Low = "Low",
}

export enum Status {
    InProgress = "InProgress",
    OnHold = "OnHold",
    Completed = "Completed",
    New = "New",
    InReview = "InReview",
    Blocked ="Blocked",
}

export enum Risk {
    Low="Low",
    Medium="Medium",
    High="High",
    Critical="Critical"
}